mixin loginForm()
  form.from(action="/login" method="POST")
    label(for="email") Email
    input(type="text", name="email")
    label(for="password") Password
    input(type="password", name="password")
    input.button(type="submit", value="Log In ")

